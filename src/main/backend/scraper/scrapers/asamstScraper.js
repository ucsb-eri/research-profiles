// Asian American Studies uses the shared UCSB Drupal "responsive grid" layout,
// so its scraper now lives in responsiveGridScraper.js. This re-export is kept
// for backward compatibility with existing imports/tests.
export { scrapeResponsiveGrid as scrapeAsianAmericanStudiesFaculty } from './responsiveGridScraper.js';
